const {addtion} = require("./sum");
const {multiplication} = require("./mul");
const {subtraction} = require("./minus");
const {divide} = require("./div");

module.exports = {
    addtion,
    multiplication,
    subtraction,
    divide
}
